export interface ImageConfigurationData {
    imageId: string;
    portfolioId: string;
}

export interface ImageConfigurationDatabaseData {
    image_id: string;
    portfolio_id: string;
    by_trader: boolean;
}